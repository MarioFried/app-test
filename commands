  docker images
  docker build -t flask-app:latest .
  docker images
  docker run -d -p 5000:5000 flask-app
  docker ps
  git add *
  git status
  git commit -m "Dockerfile - version 0.1"
  git push origin master
